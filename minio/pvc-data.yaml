---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data-claim
  labels:
    app: minio
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: "minio-data"
  resources:
    requests:
      storage: 100Gi
